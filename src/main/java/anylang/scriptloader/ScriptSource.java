package anylang.scriptloader;

/**
 * Types of Sources supported till date.
 * If you want to add to it, you must add it here first.
 */
public enum ScriptSource {

	FILE, TCP, BYTE, HTTP,AWS_S3, JPA,JDBC;
}
